# MongoDB connection string used for cloud sync/backup.
# The app is FULLY functional offline without this (Dexie/IndexedDB is the source of truth);
# set this only if you want inspections + photos to sync to MongoDB.
MONGODB_URI=
